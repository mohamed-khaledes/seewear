/** Server-only surface of the products feature. */
export {
  getCatalogFacets,
  getCategories,
  getFeaturedProducts,
  getProductBySlug,
  getProductSlugs,
  getProducts,
  getRelatedProducts,
} from "./services/api/products.server";
